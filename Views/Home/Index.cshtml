@{
    ViewData["Title"] = "Prefinal Exam - Flashcard Quiz";
}

<div class="quiz-header">
    <h1>IT Elective 2 - Prefinal Exam</h1>
    <p>Click a card to flip it and reveal the answer.</p>
</div>

<div class="card-grid" id="cardGrid"></div>

<style>
    .quiz-header { text-align: center; margin: 20px 0; }
    .card-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
        gap: 20px;
        padding: 20px;
    }
    .flip-card {
        background-color: transparent;
        width: 100%;
        height: 220px;
        perspective: 1000px;
        cursor: pointer;
    }
    .flip-card-inner {
        position: relative;
        width: 100%;
        height: 100%;
        text-align: center;
        transition: transform 0.6s;
        transform-style: preserve-3d;
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        border-radius: 12px;
    }
    .flip-card.flipped .flip-card-inner {
        transform: rotateY(180deg);
    }
    .flip-card-front, .flip-card-back {
        position: absolute;
        width: 100%;
        height: 100%;
        backface-visibility: hidden;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 16px;
        box-sizing: border-box;
        font-size: 15px;
    }
    .flip-card-front {
        background: linear-gradient(135deg, #4b6cb7, #182848);
        color: white;
    }
    .flip-card-back {
        background: linear-gradient(135deg, #11998e, #38ef7d);
        color: white;
        transform: rotateY(180deg);
        font-weight: bold;
    }
    .q-number {
        position: absolute;
        top: 8px;
        left: 12px;
        font-size: 12px;
        opacity: 0.8;
    }
</style>

<script>
    const questions = [
        { q: "What is the main problem solved by using a database instead of an in-memory collection?", a: "It allows data to persist after the application stops" },
        { q: "Which approach is being used when an existing database is used to generate EF Core entity classes?", a: "Database-First" },
        { q: "What is the primary purpose of Entity Framework Core?", a: "To map objects in code to relational database data" },
        { q: "Which EF Core component is primarily responsible for communicating with the database?", a: "DbContext" },
        { q: "What does 'dotnet ef dbcontext scaffold ...' primarily do?", a: "Generates EF Core models and a DbContext from an existing database" },
        { q: "Where is a database connection string commonly stored in an ASP.NET Core MVC application?", a: "appsettings.json" },
        { q: "A Student belongs to exactly one Section, while a Section can contain many students. What type of relationship is this?", a: "One-to-Many" },
        { q: "In the Student model, what is 'SectionId'?", a: "Foreign key referencing Section" },
        { q: "What is the purpose of a navigation property such as 'public Section Section { get; set; }'?", a: "It represents a relationship to another entity" },
        { q: "What does '.Include()' generally allow EF Core to do?", a: "Load related Section data together with Students" },
        { q: "Why might a ViewModel be used when displaying Student and Section information?", a: "To combine or shape the data specifically needed by the view" },
    ];

    const grid = document.getElementById("cardGrid");
    questions.forEach((item, index) => {
        const card = document.createElement("div");
        card.className = "flip-card";
        card.innerHTML = `
            <div class="flip-card-inner">
                <div class="flip-card-front">
                    <span class="q-number">Q${index + 1}</span>
                    <div>${item.q}</div>
                </div>
                <div class="flip-card-back">
                    <div>${item.a}</div>
                </div>
            </div>
        `;
        card.addEventListener("click", () => card.classList.toggle("flipped"));
        grid.appendChild(card);
    });
</script>